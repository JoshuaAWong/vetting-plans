---
name: vetting-plans
description: 对实现方案做把关评估，判断路径是否高效、可靠、复杂度可控，并快检需求合理性。Use when 用户给出修改/开发需求并希望评估实现方案、把关 plan、确认方案是否合理，或提到「方案把关」「评估方案」「把关方案」。Also use when evaluating or reviewing implementation plans before coding.
---

# 方案把关

对实现方案把关：路径是否高效、可靠、复杂度可控。评估对象以**方案**为主，顺带快检需求本身。

## 工作流

1. **明确输入**：用户需求 + 实现方案。若尚无方案，先简要拟定一版再评估；若用户已给方案，直接评估。涉及现有代码时，先快速核对相关现状再评——方案与现状脱节是常见风险，不读代码的评估容易空谈。
2. **需求与假设快检**（3 问）：目标是否清晰、值得做？有没有更简单的满足方式？方案依赖的假设是否显式列出，歧义处是否应先向用户澄清而非默默猜测？
3. **方案选型快检**（5 问，小改动可整体 N/A）：① 方向对因——解决的是根因还是症状？② 备选对比——考虑过哪些备选（含"不做"和"最简单做法"）？选它的理由是事实还是偏好？③ 投入产出——实现+维护成本与收益是否匹配？有没有成本差一个数量级的路线（自研 vs 现成方案）？④ 可逆性——选错代价多大、能否低成本掉头？不可逆决策需更充分论证。⑤ 契合度——与现有技术栈/架构约定是否一致？引入新范式的理由是否充分？
4. **按清单评估落地**：逐项给出 通过/有风险/N/A + 一句话理由；每项评估深度随改动规模自适应（小改动一句话带过，核心路径重点展开）。

## 评估清单（9 项：范围 → 波及 → 结构 → 质量 → 验收 → 发布）

1. **方案匹配度**（范围）：正好解决需求，无镀金、无遗漏；每处改动可追溯到需求，无顺带的重构/"改进"
2. **影响面与兼容性**（波及）：动哪些模块、波及谁、是否核心路径；是否破坏现有 API/数据格式/接口契约；涉及数据变更时，schema 迁移、历史数据兼容
3. **复用与依赖**（结构-不重复造轮子）：新功能优先复用现有模块（写新工具方法/组件前先搜现有实现）；同一逻辑复制到第 3 处应抽公共模块/方法，仅 2 处重复通常不值得抽；新增外部依赖（库、服务、中间件）必须证明值得，有无更轻替代
4. **复杂度控制**（结构-复杂度）：检验标准——资深工程师会觉得这过于复杂吗？
   - 新增复杂度是否为必要复杂度；有无为假想场景做抽象/配置/预留
   - 改动行数是否与需求成比例（小需求大改动是失控信号）
   - 单文件/函数行数显著膨胀时，是否评估过拆分——正当动机：可复用、可维护（职责单一、降低认知负担）、可测试性、变化频率分离；拆分本身有间接成本，不为拆而拆
   - 是否存在更简单的实现路径
5. **可靠性与容错**（质量）：失败路径、边界条件、幂等是否考虑；外部调用是否有超时/重试/降级
6. **安全**（质量）：新入口的鉴权、注入、敏感信息泄露风险
7. **性能**（质量）：明显隐患（N+1、循环内远程调用、大数据量退化）
8. **验证方式**（验收）：是否定义了可验证的成功标准，而不是"让它能跑"式的弱标准；改完后的验收需覆盖两点——① 正常显示/运行：无报错、无渲染/样式问题；② 符合设计预期：对照需求逐条核对行为（含边界场景），而不是只走通主路径就算完
9. **上线保障**（发布）：能否回滚（含回滚后数据怎么办）；出错时日志能否定位问题；影响面大时才考虑灰度/开关

## 输出格式（固定）

- 总体结论：方案可行 / 调整后可行 / 建议重新设计
- 改动规模预估：预计新增/修改的行数（粗估即可，如 "+300/-80"），并一句话判断是否与需求成比例
- 最多 3 个关键问题及修改建议
- 简化建议：可删减、可复用、可延后的部分

## 使用示例

触发："方案把关一下这个方案：给许可系统加批量导入，准备用 MQ 异步 + 定时对账……"

输出节选：

> 结论：调整后可行。
> 改动规模预估：+150/-30 行，与批量导入需求成比例。
> 关键问题：① MQ 对 500 条/批的内部导入属超配，同步 + 事务即可（清单 4）；② 未定义失败重传的成功标准（清单 8）；③ 导入入口未见鉴权与文件类型校验（清单 6）。
> 简化建议：砍掉 MQ 和对账任务，用同步导入 + 行级错误报告，后续量大了再升级。

## 原则

- 评估深度与改动规模成正比，小改动不过度评审
- 不适用的清单项标记 N/A 即可，不强行挑刺
- 区分必要复杂度与过度设计，复杂度超配必须指出
- 优先复用现有组件，警惕新依赖
- 发现更简单的路径必须明确提出异议，而不是顺着方案评
- 假设和歧义摆到台面上：该澄清的让用户决定，不替用户猜
